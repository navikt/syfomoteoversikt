name: main

on: push

jobs:
  build-and-deploy:
    permissions:
      contents: read
      id-token: write
    uses: navikt/isworkflows/.github/workflows/node-build-deploy.yml@use-cache-setup
    with:
      node-version: 18
      distroless: true
    secrets: inherit
